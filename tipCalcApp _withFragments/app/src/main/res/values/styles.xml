<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Customize your theme here.  -->
        <item name="colorPrimary">@color/navyBlue</item>
        <item name="android:windowBackground">@color/mint</item>
        <item name="android:textViewStyle">@style/textStyling</item>
        <item name="android:buttonStyle">@style/Widget.AppCompat.Button</item>
    </style>

    <!-- Auto implemented styles -->

    <style name="textStyling" parent="android:TextAppearance">
        <item name="android:textColor">@color/navyBlue</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:typeface">sans</item>
    </style>

    <style name="Widget.AppCompat.Button" parent="android:Widget.Button">
        <item name="android:background">@color/navyBlue</item>
        <item name="android:textColor">@color/mint</item>
        <item name="android:textSize">25sp</item>
    </style>

</resources>
